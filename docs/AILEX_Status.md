**AILEX — Текущий статус и приоритеты**

*Рабочий документ-ориентир. Обновляется по ходу работы.*

Последнее обновление: 30.06.2026 (v60)

Текущая спецификация: docs/AILEX_4_24.md

*Назначение файла: одним взглядом показать, что уже сделано, что внедряется и что делать дальше. Этот документ — первый источник для проверки статуса: не предлагать как новое то, что уже реализовано.*

# 1. Реализовано в основной версии (index.html v44, работает)

- Цикл урока из 21 задания (термин «дрилл» устаревший, заменяется на «задание»).

- Placement Test v2 (реализован в v33): 3 вопроса на уровень × 5 уровней = до 15 вопросов; поле skill: grammar | vocab; раздельные счётчики wrongChoice / dontKnow; остановка при wrongChoice>=2 ИЛИ wrongChoice+dontKnow>=3; порог подтверждения 2 из 3; маршрут passed['A1']=0 → A0.1.01. (v1 с 2 вопросами — устарел, заменён.)

- Нарративные квесты с развилками (2 ветки × 2 выбора).

- Уровень A0 — Pre-A1 (реализован в v33): 9 тематических блоков (~12–15 уроков по дистанции письменности); контракт Gemini a0_lesson с items/mini_check/mini_scene; алфавитные блоки (1–2) — только фонетические упражнения (буква↔звук, 12 шт.), без лексики; блоки 3–9 — situated learning с мини-сценами; автопереход на A1.1.01 после последнего урока; Суперквест на A0 недоступен.

- Суперквест (заменяет «Супер-урок», v31) — единый квест-погружение на 30–50 предложений: 3–5 актов по CEFR с экраном-итогом между ними, гибкие развилки (не больше 1–2 на акт), тап-перевод новых слов, Anti-Frustration по актам, итоговая точность за весь Суперквест. Проверено сквозным прогоном.

- Словарь с мнемониками тир 1 (текст + эмодзи).

- Экран аналитики / прогресса (кольцо по урокам уровня, точность отдельной подписью).

- Голосовой диалог (STT → Gemini → TTS).

- Движок адаптации «Gemini советует — приложение решает».

- Локальная база на устройстве (AILEX_StatusDB); экспорт/импорт через JSON.

- Несколько профилей на устройстве (один профиль = одна языковая пара).

- Перебор моделей Gemini с устойчивостью к сбоям и лимитам 429 (thinkingBudget = 0, запоминание рабочей модели).

- PWA-оболочка (manifest, sw.js с версионированием кэша); APK собран через PWABuilder (TWA); опубликовано на GitHub Pages.

- Расширенный список языков (v35): 72 языка (было 15) — все языковые пары, которые Gemini знает достаточно для обучения. Поле bcp у каждого языка: BCP-47 для TTS, либо null если надёжной озвучки нет.

- TTS с деградацией без ошибок (v35): если у целевого языка bcp=null — кнопки 🔊 скрываются везде (словарь, Grammar Card, квест, суперквест, sayBtn); speak() и makeRecognizer() молча возвращают управление; транскрипция родными буквами остаётся единственной опорой. Текст подсказки на экране новых слов адаптируется («транскрипция — ваша главная опора» вместо «эталон даёт озвучка»).

- Виджет выбора языка с поиском (v35): оба экрана выбора языка (родной и изучаемый) теперь отображают поле фильтрации — фильтрует список по мере набора (по названию или коду языка). select заменён на кастомный список lang-opt с hidden input. Дефолтные значения подставляются автоматически.

- Механика заданий с пропуском v1 (v36, спецификация 4.20): drillReviewContext и drillContext принимают полное предложение; формулировка «Напишите предложение, подставив пропущенное слово»; два пути проверки: (1) полное предложение → вырезать слово по позиции пропуска, (2) одно слово → засчитать + показать полное предложение; после верного ответа — полное предложение + 🔊.

- Фикс multi-word extraction (v37): баг извлечения заполненного слова из полного предложения для многословных ответов (например, «das Hotel») — исправлен.

- Фикс answer key completeness (v38): промпт Gemini получил явное требование — answer_key должен быть грамматически полным предложением на целевом языке (с артиклями, падежами и т.д.). Ранее Gemini мог опускать артикли, что вызывало ложные ошибки у пользователя.

- Фикс дистракторов в drillRecog (v38): фильтр на seen=true, длина < 40 символов, отсутствие кириллицы и скобок/стрелок в поле term — исключает несмотренные импортированные слова и заголовки словарей («СЛОВАРЬ ТЕРМИНОВ ЧПУ OKUMA (нем. → рус.)»).

- Инструкция в drillRecog (v38): добавлена строка «🎯 Какое слово соответствует: «…»?» — задание стало явным.

- Попап тап-слова (v39): добавлена кнопка ✕ для явного закрытия (ранее закрывался только тапом вне попапа). Закрытие тапом по фону сохранено как дополнительное.

- Транскрипция родными буквами в квестах (v39): промпты quest_step для квеста и суперквеста ужесточены — явный запрет IPA, обязательная транскрипция символами native_lang с конкретным примером [форбэрайтн]. Gemini больше не может подставлять IPA-символы.

- Расширенный пул запасных моделей Gemini (v40): было 2 модели, стало 6 запасных — приложение перебирает их по очереди с паузой между попытками, без агрессивного спама к API.

- Фикс Суперквеста при обрыве соединения (v40): Суперквест больше не засчитывается как «пройденный» если соединение оборвалось до начала первой сцены. Показывается экран «Нет связи — попробовать снова».

- Компактный попап тап-слова (v41): убрано затемнение всего экрана при тапе на слово. Попап теперь — тонкая плашка внизу экрана, фон остаётся виден. Формат: «слово 🔊 · [транскрипция] · перевод · ✕». TTS-кнопка сохранена в компактном виде (иконка рядом со словом, не кнопка на всю ширину).

- Тап-перевод всех слов в квестах и суперквестах (v41): ранее подсвечивались только новые слова из newGloss. Теперь все значимые слова кликабельны (кроме служебных: артикли der/die/das/ein, союзы und/oder/aber и т.п.). Логика: сначала поиск в словаре пользователя (мгновенно) → если нет, запрос к Gemini (~1 сек с индикатором) → новое слово автоматически сохраняется в словарь со статусом «встреченное». Встреченные слова текущего урока кэшируются в L.encounteredCache для быстрого повторного поиска.

- Онбординг-подсказки (v41): система одноразовых оверлеев при первом входе на экран. Флаги хранятся в localStorage (ключи hint_shown_*). Закрываются кнопкой «Понятно ✓» или тапом по фону. Покрыто 6 экранов: новые слова урока, задания (drill), квест, суперквест, словарь, главный экран (прогресс-кольцо). Тексты подсказок в объекте HintTexts.

- Приоритет модели Gemini (v43): порядок перебора изменён — `gemini-2.0-flash` теперь первый (быстрее 2.5-flash в ~3 раза при сопоставимом качестве для A1). Порядок: `2.0-flash → 2.5-flash-lite → 2.5-flash → 2.0-flash-lite → 1.5-flash → 1.5-flash-8b`. Решение: скорость ответа важнее максимальной точности модели для контента уровня A1–B1.

- Параллельный запрос при старте суперквеста (v43): запрос `functional_new` к Gemini запускается через `Promise` сразу при входе в `startSuperLesson()`, не блокируя подготовку данных. Сокращает время до первого экрана суперквеста.

- Фикс попапа тап-слова — пустые поля (v43): `lookupAndShowWord` теперь проверяет полноту записи в словаре (`translit || translation`). Если запись есть, но поля пустые — идёт к Gemini, получает данные и дополняет существующую запись. Ранее попап показывал только слово + 🔊 для записей с пустыми полями.

- Дополнение словарных записей (v43): при тап-запросе к Gemini (`translate_word`) — если слово уже в `DB.vocab` с пустыми `translit`/`translation` — поля дополняются (не перезаписываются), `Storage.save()` вызывается только при фактическом изменении.

- Фикс обрыва сети в суперквесте (v43): при сбое соединения на сцене 2+ (не первой) вместо немедленного `finishSuper()` показывается экран «Связь прервалась» с кнопками «Повторить сцену» и «Завершить Суперквест». Пользователь сам решает продолжать или нет. `finishSuper()` при обрыве на сцене 1, акте 1 по-прежнему не вызывается — показывается экран «Нет связи» с повтором старта.


- **Чистка списка моделей Gemini — критический фикс (v44):** в коде были прописаны мёртвые модели, отключённые Google. `gemini-2.0-flash` и `gemini-2.0-flash-lite` отключены 01.06.2026; `gemini-1.5-flash` и `gemini-1.5-flash-8b` — архивированы/отключены (404). В v43 `gemini-2.0-flash` по ошибке стояла ПЕРВОЙ в очереди → приложение упиралось в мёртвый эндпоинт, перебирало дохлые модели с паузами и ретраями → отсюда загрузка по 10-15 секунд. Новый список — только живые модели, порядок по лимитам Free Tier: `['gemini-3.1-flash-lite', 'gemini-3.5-flash', 'gemini-2.5-flash']`.
  - `gemini-3.1-flash-lite` — первая: 30 RPM / 1 000 000 TPM / 1500 RPD. Самые щедрые лимиты Free Tier, в 429 упереться почти невозможно.
  - `gemini-3.5-flash` — фолбэк: 15 RPM / 250k TPM / 1500 RPD. Умнее, без объявленной даты отключения.
  - `gemini-2.5-flash` — последний фолбэк: 10 RPM / 250k TPM / 1500 RPD. EOL 16.10.2026 — потом удалить.
  - Рассмотренные и отклонённые альтернативы: алиас `gemini-flash-latest` (рекомендация шпаргалки — не устаревает, но непрозрачно какая модель отвечает, хуже для отладки методики); `gemini-2.5-flash-lite` (лимиты не указаны/ниже, чем у 3.1-flash-lite — смысла нет).

- **maxOutputTokens — решено НЕ ставить (v44):** проведён расчёт расхода токенов. Обычный урок A1 ≈ 11 500 токенов / ~19 запросов; суперквест (30 сцен) ≈ 56 000 токенов / ~61 запрос; средний день пользователя (2 урока + 1 суперквест) ≈ 80 000 токенов / ~99 запросов. Против лимитов 3.1-flash-lite: RPD 99/1500 = 6.6%, дневной расход токенов = 8% от ОДНОминутного лимита TPM. Запас огромный. Лимит maxOutputTokens создал бы только риск обрезанного JSON — оставлен без ограничения.

- **Анализ узкого места — RPM, не токены (v44):** реальное (и единственное) ограничение для AILEX — частота запросов в минуту (RPM), а не объём токенов и не дневной лимит. Суперквест делает ~61 запрос; при быстрой игре (сцена за 20-30 сек) можно превысить 30 RPM у 3.1-flash-lite или 10 RPM у 2.5-flash → 429. Решается выбором lite-модели с 30 RPM первой + существующей логикой пауз при 429 (ждём 2с→4с, повтор той же модели до 3 раз, иначе следующая модель). Логику ретраев менять не потребовалось.

- **Фикс UX упражнения «Слово в контексте» (v45):** по результатам тестирования выявлено три проблемы в `drillContext`:
  1. **Перевод предложения** — под предложением с пропуском теперь всегда показывается перевод на родной язык (`📝 ...`) из поля `item.prompt_native`. Ранее перевода не было, пользователь видел только слово-подсказку без контекста всей фразы.
  2. **Двухуровневая лампочка** — автоподсказка для «трудных» слов (`hardWord`) убрана полностью (показывалась сразу, не давая попробовать самому). Теперь: первый тап 💡 → скелет (первые 2 буквы) + транскрипция; второй тап → полное слово + транскрипция + перевод. Отклонено: одноуровневая подсказка (не даёт прогрессивного раскрытия).
  3. **TTS в «Фразах на каждый день»** — кнопки 🔊 не срабатывали из-за конфликта механизмов: `drillPhrases` использовал `data-t` + querySelectorAll('.tts'), тогда как везде в приложении используется `data-say` + `onclick="replaySay(this)"`. Исправлено унификацией на стандартный механизм.

- **Кэш незавершённого урока (v45):** добавлен объект `LessonCache` (save/load/clear). Весь объект `L` (слова, задания от Gemini, текущий шаг) сохраняется в localStorage при каждом переходе между шагами. При релоде/выходе из приложения урок восстанавливается мгновенно без обращения к Gemini. В главном меню появляется кнопка **▶️ Продолжить урок X.X.XX** (Задание N из M) если есть незавершённый урок не старше 24 часов. Кэш очищается при завершении урока (`finishLesson`) или при старте нового урока с другим кодом. Ключ кэша включает версию приложения (`AILEX_lesson_cache_v45`) — при обновлении приложения старый кэш автоматически игнорируется. Объём кэша: ~10-15 КБ на урок. Суперквест не кэшируется (сложная структура с промисами). Отклонено: восстановление суперквеста (слишком сложная сериализация state machine с параллельными запросами).


**Реконструкция истории v46–v52** *(сведено 29.06.2026 по чатам сессий)*

- **v46:** авторасширяемое поле ответа (textarea ans-grow + bindAutoGrow) перенесено в обычные уроки (drillContext/drillReviewContext) — длинные предложения не обрезаются (паритет с drillConstruct/квестами).

- **v47–v49 — педагогическая переработка** *(версии частично перемаркированы в ходе сессии: v48 → v49):*
  - grammar_rule/construction: правила простым языком, контраст форм и словарная/инфинитивная форма (QUEUE-01/12); строгая симметрия prompt_native↔expected_target в drillConstruct (QUEUE-02/09); known_rules передаётся — грамматика задания ограничена пройденным (QUEUE-05/11).
  - drillContext: засчёт любой корневой формы пока правило не пройдено (QUEUE-05); разбор формы при ошибке (QUEUE-04); лампочка показывает форму задания, не инфинитив (QUEUE-03).
  - drillPhrases: фикс «белых квадратов» TTS (QUEUE-06); пары реплика+ответ (QUEUE-08); ротация фраз из словаря при <3 от Gemini (QUEUE-07).
  - quest/superquest: полный чёрный список A1-грамматики (модальные, отделяемые, Dativ, Perfekt, um…zu, Genitiv, Relativsatz) + known_rules; задание только из содержимого сцены (QUEUE-22); одно предложение на A1 (QUEUE-20); scaffold обязателен на A1–A2 (QUEUE-17).
  - принцип перевода: корректно по-русски, не калька, сохраняя смысловые слова (QUEUE-15); tap-перевод — попап у тапнутого слова (QUEUE-18).
  - запрет техкодов (G01/T010101) как слов урока (QUEUE-23).

- **v50:** усиление allow_creative в check_answer квеста и суперквеста — семантически верные альтернативы засчитываются, не требуется точное совпадение с expected_target (QUEUE-13/24).

- **v51:** scene_text грамматически безупречен + единый контекст сцены (QUEUE-25/26); синонимы через слэш в glossary — любой один вариант верен (QUEUE-27); правило is_final=true только при scene_index===max_scenes (промпт-часть) + перевод вариантов развилки и усиление нарратива по story_so_far (QUEUE-28/29); приоритет target_lang в tap-переводе словоформ (QUEUE-19, частично).

- **v52:** включение выбора пользователя в summary суперквеста (choiceNote → story_so_far) (QUEUE-30 для суперквеста); уточнение формулировки prompt_for_user (на чём/как, не куда) (QUEUE-31); доработки каркаса и слов задания (контекст к QUEUE-32/33).

> *Не закрыто, несмотря на внедрение (адресуется в v54):* QUEUE-09 (симметрия), QUEUE-11/16 (грамматика выше A1 — Gemini игнорирует правило), QUEUE-19 (локальный поиск берёт чужую форму раньше слов сцены), QUEUE-28 (код-защита через ИЛИ пропускает ранний is_final), QUEUE-30 (в обычном квесте choiceNote в summary не добавлялся — только в суперквесте).

> *Оговорка:* границы версий v46–v52 реконструированы по чатам (озвучка→текст); часть правок переносилась между перемаркированными билдами. Список отражает что было сделано, не точную построчную привязку к номеру.

- **Каркас на вопросах-выборах + точность грамматики в фидбэке (v53, QUEUE-32):** на задании-выборе («ручной режим или автоматику?») scaffold выдавался как «Ich nutze den ___» — артикль den вынесен за прочерк, подходил только для der Handbetrieb; для die Automatik навязывал неверное «den Automatik» и выдавал род (открытый выбор превращался в ложный). Плюс check_answer в разборе приписал Automatik средний род (на деле женский). Правки: (A) в промптах quest_step и superquest — при выборе из слов разного рода/падежа артикль НЕ выносится за прочерк, входит в прочерк вместе с существительным («Ich nutze ___.»); видимая часть каркаса одинаково верна для любого варианта. (B) в check_answer квеста и суперквеста — explanation/rule_* обязаны быть фактически верны (род/падеж/спряжение), при задании-выборе любой вариант с грамматически верным артиклем = correct=true. Отклонено: чинить каркас на стороне кода (срезать определитель перед ___) — хрупко, род приложению неизвестен → слой промпт.

- **Слова задания с артиклем рода (v53, QUEUE-33):** в «📒 Слова задания» существительные показывались без артикля (учащийся не знает род → не образует падеж). Правка C: в answer_glossary и encountered_words промптов quest_step/superquest поле term для существительных целевого языка обязано включать артикль рода в словарной форме (die Automatik, der Handbetrieb); прилагательным/наречиям/глаголам — без артикля; в безартиклевых языках — без артикля. Компаньон в коде: совпадение в кэше тап-перевода (lookupAndShowWord) сделано артикль-терпимым (срезается ведущий der/die/das с обеих сторон) — тап по «Automatik» мгновенно находит «die Automatik» без досыла к Gemini. Опирается на уже существующую спеку «Паспорт слова» — спека не меняется.

- **Три фикса нарратива и тап-перевода (v54):**
  - **QUEUE-28** (квест/суперквест завершались досрочно): код-защита была через ИЛИ (`isFinal = sc.is_final || Q.scene >= max`) — ранний `is_final` от Gemini всё равно обрывал историю. Заменено на строгий счётчик: квест — при `Q.scene >= maxScenes`, акт суперквеста — при `Q.scene >= scenesInAct`, вся история — на последней сцене последнего акта; сигналы `is_final`/`is_act_final` от Gemini больше не учитываются.
  - **QUEUE-30** (обычный квест повторял сцену после развилки): в `advance()` квеста выбор ветки (`choiceNote`) теперь добавляется в `Q.summary` (story_so_far) — как уже было в суперквесте. Gemini знает выбранную линию и продолжает её.
  - **QUEUE-19** (тап давал чужую форму, «plane» → «самолёт»): в `lookupAndShowWord` порядок поиска изменён — сначала слова текущей сцены (sceneWords), затем общий словарь, затем Gemini. Контекст сцены побеждает общий словарь (`target_lang` в запрос к Gemini уже передавался).

- **QUEUE-09 и QUEUE-11/16 — решение «без нового кода» (v54):** промпт-правила симметрии (09) и CEFR-потолка (11/16) в drillConstruct и квесте уже выжаты по максимуму, Gemini периодически их игнорирует. Построчная код-сверка слов (09) и язык-специфичный пост-фильтр конструкций (11/16) сознательно НЕ внедряются — хрупкие, сами дают ложные блокировки. Вместо запрета — поддержка: каркас показывает структуру, тап даёт слова, лампочка раскрывает ответ, проверка ответа мягкая («любой грамматически верный по смыслу = correct=true») и не блокирует прогресс. Симптом (блокировка) снят; пункты остаются открытыми как «mitigated подсказками», вернуться при смене языка/уровня.

- **Сюжетный движок Суперквеста — новелла вместо описания комнаты (v55):** корень репетативности — `must_use` считался раз на акт и шёл в каждую сцену с требованием «используй преимущественно эти слова»; статичный сценарий → Gemini пересказывал ту же ситуацию. Внедрён двухслойный движок (спека ред. раздела 6, 4.21): (1) новый task `generate_story_arc` — один раз на старте строит скелет истории (logline, уникальный hero{name,role,voice}, tone, beats по числу сцен: place/action/shift/twist + точки развилок), хранится в `L.superStory.arc`; (2) сцена получает свой `beat` (глобальный индекс сквозь акты) + hero + tone и РАЗВОРАЧИВАЕТ конкретный бит, а не описывает ситуацию заново. `must_use`/`vocab_balance` смягчены до мягкой опоры — новые слова разрешены и желательны, сюжет важнее повторения; CEFR держит только грамматику задания. Запрет выдачи ответа: `prompt_for_user` не содержит готовый эталон (фикс «Используйте структуру X ist Y»), структура-подсказка — только scaffold. Решения: тон всегда живой; герой уникален под каждый суперквест; арка генерится сразу на весь суперквест (токены позволяют — биты терсовые). Адресует QUEUE-26 (связность) и QUEUE-29 (сюжет вместо набора фраз). Graceful fallback: при сбое арки — прежний режим по story_so_far. Полная спека: docs/AILEX_4_21.md, раздел 6.

- **Лампочка «Показать ответ» в сцене квеста и суперквеста (v56, QUEUE-35):** в тренировках лампочка 💡 была, а в сцене квеста/суперквеста — нет: полный ответ всплывал только после отправки, «подсмотреть фразу целиком не отправляя наугад» было негде. Добавлена кнопка 💡 (рядом с 📐 и «Отправить») — по тапу раскрывает expected_target целиком в #fb с озвучкой (sayBtn), считается как подсказка (L.hints++). Различие закреплено: запрет выдачи ответа (v55) убирает эталон только из ТЕКСТА задания (prompt_for_user); каркас, 📒 «Слова задания» (слова для прочерков с переводом/транскрипцией) и лампочка остаются — словарь искать не нужно.

- **Вопрос по элементу сцены — двухуровневый тап-попап «почему так?» (v57, спека 4.22):** тап-перевод давал значение слова, но не объяснял, почему слово/форма стоит в предложении именно так (пример: «ein» в «Ich gebe die Daten ein» — оторванная приставка глагола eingeben; тап по «ein» бесполезен, нужен грамматический разбор). Добавлен второй уровень: в попап тап-перевода (только при тапе внутри сцены, где есть контекст предложения) добавлена кнопка 📘 «почему так?». Перевод по-прежнему мгновенный (первый уровень, из словаря); правило подтягивается ТОЛЬКО по тапу 📘 — новый task `explain_element` к Gemini получает целевое предложение (sentence) + выделённый элемент (element) + CEFR, возвращает объяснение простым языком на native_lang без лингвистических терминов (аудитория — дети и взрослые без подготовки) + короткий пример той же конструкции с озвучкой. Правило разворачивается в том же попапе (`#tapWhyBox`), попап переразмещается (`clampPopup`) чтобы влезть в экран; повторный тап сворачивает/разворачивает. RPM не страдает — лишний запрос лишь по факту нажатия, перевод первого уровня остаётся локальным и мгновенным. Методически чисто: правило (в т.ч. выше текущего уровня) ВЫТЯГИВАЕТ сам пользователь по запросу — это не давление контентом (в отличие от QUEUE-11), а добровольный разбор; работает и как клапан, когда контент уезжает выше уровня. Отклонено: показывать правило ВМЕСТО перевода (теряется быстрый тап-перевод, который дотачивали с v39 по v54) и совмещать перевод+правило в одном запросе (каждый тап шёл бы в Gemini, теряя локальную скорость). Полный контракт: docs/AILEX_4_22.md, раздел 2.5, Пример G.

- **Узуальность фраз сцены/эталона — QUEUE-34 (v57):** Gemini строил формально-правильные, но неупотребимые конструкции («das Ankratzen im Manuell» вместо «im Handbetrieb»/«manuell») — учащийся заучивал кальку. Бьёт и по сцене (читают/слушают как образец), и по эталонному ответу. В промпты quest_step и superquest добавлено требование из трёх частей: (1) scene_text и expected_target обязаны быть такими, как реально говорит носитель, а не дословной сборкой слов; (2) шаг самопроверки перед выдачей («так действительно говорят в живой речи?» → переформулировать); (3) пример контраста калька↔живое прямо в промпте. Честный нюанс: это промпт-правила, Gemini их периодически игнорирует (как QUEUE-09/11) — но узуальность нельзя проверить кодом (язык-специфичных списков «так не говорят» не составить), промпт здесь единственный рычаг. Проверяем на практике.

- **Авто-TTS при показе полного ответа — QUEUE-10 (v57):** озвучка при раскрытии ПОЛНОГО ответа через лампочку 💡 срабатывала непоследовательно (в drillConstruct сама запускалась, в других заданиях — нет). Унифицировано: при показе полного ответа TTS запускается автоматически во всех заданиях — drillContext (второй уровень лампочки, добавлен и sayBtn), drillConstruct, сцена квеста, сцена суперквеста. Промежуточные подсказки-скелеты (первый уровень лампочки, «первая буква») озвучку НЕ запускают — только полный ответ. Отклонено: нигде не запускать само, только по кнопке 🔊 (лишний шаг после раскрытия ответа).

- **Углублённое объяснение в попапе «почему так?» — двухуровневый ответ (v58, спека 4.23):** разбор по кнопке 📘 был коротким (1–3 предложения) — пользователь просил подробнее (на примере «mit Sorgfalt»: почему без артикля). Контракт explain_element расширен: Gemini возвращает ДВА уровня в одном ответе — `explanation` (короткая суть, показывается сразу) и `detail` (развёрнутый разбор по фиксированной структуре: почему так → 2–3 примера той же модели с переводом → контраст «когда было бы иначе»). В попапе `detail` скрыт за кнопкой «подробнее ▼», разворачивается локально (второго запроса к Gemini НЕТ — оба уровня уже получены, RPM не страдает); блок правила сделан прокручиваемым (max-height 42vh) на случай длинного текста. Ключевое ограничение усилено: СТРОГО без лингвистических терминов даже в подробном разборе — задан явный список запретов («нулевой артикль», «абстрактное существительное», «наречный оборот», «отделяемая приставка»…) с заменами на смысл («слово-свойство», «здесь артикль не нужен», «отвечает на вопрос как?», «кусочек глагола уехал в конец»). Это прямой ответ на риск: подробный разбор от Gemini тянет за собой жаргон, а методика (аудитория — дети и без подготовки) его запрещает. Отклонено: вариант с прокруткой длинного текста без сворачивания (вариант А) — выбран Б (коротко по умолчанию + «подробнее»), но реализован одним запросом вместо двух.

- **Узел повторения в контексте + анти-коллизия квеста + тап-каркас (v59, QUEUE-36/37/38/39/40):** пакет из теста урока и квеста, пять связанных фиксов.
  - **QUEUE-38 — в повторение попадали невыученные слова.** Корень найден на коде: `recent = DB.vocab.slice(-10)` тянул в recap последние ДОБАВЛЕННЫЕ слова, включая пассивно встреченные (тап в сцене → status «встреченное»; глоссарий/encountered квеста → «встречено в контексте»). Фикс: в recapPool добавлен фильтр `learnedOk` — слова со статусом пассивной встречи (или `seen_in_context`) в повторение НЕ идут; исключение — слова из reviewQueue (трудные, их дрилили). Версия «маленький словарь добивает нехваткой» отвергнута (словарь 200+).
  - **QUEUE-40 — многозначное слово подписано не тем значением.** «Futter» (корм / патрон токарный) попало в словарь как «патрон» из техквеста, а generate_review_sentences сгенерировал предложение про корм для собаки → рассинхрон. Фикс: reviewWords хранят перевод; в generate_review_sentences передаётся `{word, meaning}`, и Gemini обязан построить предложение В ЗНАЧЕНИИ meaning (контекст соответствует значению). Ведущий принцип — контекст предложения первичен.
  - **QUEUE-39 — подсказка показывала обрезок.** Скелет «fr·· фро» заменён на полное слово + транскрипция + перевод + озвучка (как лампочка полного ответа в других заданиях). Скелет в drillReviewContext убран; в drillContext («Слово в контексте» — другое задание) скелет оставлен (не репортилось).
  - **QUEUE-36 — сцена праймит неверное завершение, ответ невыводим из контекста.** Два подтверждённых случая (суперквест: wähle das Ziel; обычный квест: bezahle das Ticket→эталон bezahle den Ausflug). Фикс — два правила в промпты quest_step И superquest: (1) анти-коллизия — конструкция, которую просят достроить, НЕ должна встречаться в сцене уже достроенной с другим объектом; (2) явный набор ответов — при выборе из перечисленных слов prompt_for_user прямо называет ограниченный набор. Промпт-правила (как QUEUE-34), 100% гарантии нет — проверяем на практике.
  - **QUEUE-37 — тап-перевод по словам каркаса.** Слова каркаса обёрнуты в `tapAllWordsHTML` (квест и суперквест) — переиспользуются существующие тап-спаны и обработчик. Прочерк и поле ввода не трогаются. Кнопка 📘 «почему так?» в каркасе НЕ появляется автоматически: каркас вне контейнера `.target`, поэтому контекст для explain_element пустой → только перевод (ровно согласованный вариант «а»).
  - Контракт generate_review_sentences (вход: review_words теперь объекты `{word, meaning}`) изменён, но в спеке он не документирован (внутренний) — подъёма спеки не требует, осталась 4.23.

- **Объяснение по форме из sentence + валидатор согласованности сцены (v60, QUEUE-41/42, спека 4.24):**
  - **QUEUE-41 — explain_element объяснял грамматику примера, а не разбираемой фразы.** Тап по «Handbetrieb» в «Ich schalte das Gerät auf Handbetrieb» (без артикля) → объяснение «используем den», хотя den в этой фразе нет (он был только в поле example). Фикс промпта: разбирать РОВНО форму из sentence (нет артикля → не объяснять артикль), example — другое предложение, его грамматика не подменяет разбор; иная форма примера — только как явный контраст.
  - **QUEUE-42 — приложение проверяет согласованность сцены (промпт-фиксы протекали).** На v59 после промпт-фикса QUEUE-36 коллизия и рассинхрон вопрос↔эталон воспроизвелись снова → решено подкрепить кодом+валидатором, а не уговорами. Реализован новый контракт `validate_quest_scene`: вызывается 1 раз после генерации каждой сцены (квест и суперквест) перед показом. Два слоя: СТРУКТУРНАЯ проверка кодом (стем каркаса есть в сцене достроенным иначе → `structural_collision`, ловит праймящую коллизию детерминированно) + СЕМАНТИЧЕСКАЯ через Gemini (по выбору пользователя — локально смысл не проверить). Валидатор при нарушении возвращает исправленные prompt_for_user/expected_target/scaffold/answer_glossary, приложение их подменяет; graceful fallback при сбое (исходная сцена, поток не блокируется).
  - **Расширение валидатора (фолд из теста v59, скрин «das Programm weiterlauf»):** в тот же `validate_quest_scene` добавлены ещё два критерия — (3) узуальность и корректность слов (битые/несуществующие слова вроде «weiterlauf» и кальки → упростить под CEFR) и (4) согласованность глоссария (answer_glossary и форма в эталоне совпадают; при задании-выборе ≥2 варианта, иначе выбор фиктивный). Итог: валидатор — единая точка контроля согласованности сцены (смысл + коллизия + узуальность + глоссарий), а не четыре промпт-заплаты по очереди.
  - Стоимость: +1 запрос Gemini на сцену. По RPM на штатной игре (сцена раз в 30–60 с) в потолок не упирается.

# 2. Спецификации — статус реализации

- Спецификация 4.17 (Placement Test v2, A0) — полностью реализована в коде v33.
- Спецификация 4.18 (баг A0-алфавита, идиоматичная локализация) — полностью реализована в v34.
- Спецификация 4.19 (расширение языков, TTS-деградация, виджет поиска) — полностью реализована в v35.
- Спецификация 4.20 (механика заданий с пропуском) — полностью реализована в v36–v37.

# 3. Зафиксировано в спецификации, внедряется в код поэтапно (4.9–4.14)

- Паспорт слова: формы глаголов (инфинитив/претеритум/партицип II) и существительных (артикль рода, мн. число).

- Точное правило под конкретную ошибку, показывается сразу.

- Прогресс по уровню CEFR; переход на уровень — через экзамен.

- Каталог словаря по первой букве слова без артикля; фильтр «только неправильные глаголы».

- Человечная проверка по смыслу (синонимы, опечатки, артикли не блокируют; подсветка места ошибки).

- Подсказки-скелеты с угасанием, прячущие целевой элемент задания.

- Полная подсказка: правильный ответ с транскрипцией, переводом и озвучкой.

- Слой повседневных выражений (Alltagsphrasen) — фразы-клише с переводом, транскрипцией, контекстом, озвучкой.

- Импорт своего словаря (copy-paste) и автоматический режим интенсива под профессиональную лексику.

- Локализация интерфейса по системному языку с автопереводом через Gemini и кэшем.

- Идиоматичная локализация терминов (реализована в v34): промпт передаёт Gemini контекст и функцию каждого термина (таблица UI_CTX), Gemini подбирает идиоматичное слово языка — не дословный перевод. «Recap» переименован в «Повторение» во всём коде.

# 4. Параллельная заготовка — НЕ в разработке (решение не принято)

Гибридная версия существует только как спецификация. Решение о платной модели НЕ принято и будет приниматься после собственного тестирования и фокус-группы.

- Прокси Cloudflare (worker.js) — маршруты /generate и /check.
- Freemium: 10 бесплатных уроков, затем ввод своего ключа или подписка.
- Подписка, реклама (AdSense/AdMob).
- Публикация в Google Play.

# 5. Будущие этапы (ориентир, не сейчас)

- Облачная синхронизация (Google Sheets / Drive, OAuth).
- Мнемоники тир 2 (схемы из данных) и тир 3 (Imagen-картинки).
- Нативная Android-версия (Kotlin / Flutter), push-уведомления, оценка произношения.

# 6. Текущий фокус и ближайшие шаги

- Тестирование v57 на устройстве: двухуровневый тап-попап «почему так?» (explain_element), узуальность фраз сцены/эталона (QUEUE-34), авто-TTS полного ответа во всех заданиях (QUEUE-10).
- Финализация и реализация сюжетного движка Суперквеста (спека раздел 6, ред. 4.21–4.22).
- Идиоматическая локализация UI: Gemini получает термин + описание контекста/функции и подбирает естественное слово на язык (не дословный автоперевод); язык-источник — русский; ручные таблицы перевода отклонены.
- Доведение методики уроков A1+ в основной версии (index.html).
- Собственное тестирование функционала.
- Тест на фокус-группе.
- Решение о модели монетизации — по итогам тестирования.

# 7. Очередь работ (только актуальное)

*История закрытых пунктов — в git и в разделе 1 (записи по билдам). Здесь только то, что ещё НЕ закрыто кодом.*

**Закрыто и вычищено из очереди:** QUEUE-01–35 — внедрены в v47–v57. QUEUE-09 и QUEUE-11/16 — «mitigated подсказками», код-фикс сознательно не делается (раздел 1, v54). QUEUE-10, QUEUE-34 — v57. QUEUE-36, 37, 38, 39, 40 — v59 (узел повторения + анти-коллизия + тап-каркас, детали в разделе 1). QUEUE-41, 42 — v60 (разбор формы из sentence + валидатор согласованности сцены validate_quest_scene, детали в разделе 1).

**Открыто — проверить на тесте (кода не требует):**

- **QUEUE-14/15 — баланс перевода (прозрачность vs дословность).** Принцип реализован в v47–v49: перевод корректный по-русски, сохраняет все смысловые слова, но служебные глаголы (sein/быть) опускаются, порядок слов русский, непереводимые конструкции — с пояснением в скобках. Остаётся подтвердить на живом тестировании, что баланс выдержан (не калька и не вольный пересказ). Нового кода не требует — только наблюдение; при перекосе вернуться к формулировке промпта перевода.

# 8. Базовые принципы (рамка, неизменно)

- Логика → приложение, контент → Gemini, хранилище → локальная база на устройстве.
- Смена версии модели Gemini НЕ меняет методику обучения.
- Gemini — только генератор контента: слова, фразы, проверка, квесты — всегда строго в JSON по контракту. Методику не знает и не определяет.
- Принцип терминологии: методические термины в интерфейсе — на родном языке учащегося.

# 9. Известные расхождения (исправленные и открытые)

**Исправлено в 4.17:**
- Status ранее утверждал «18 вопросов» в placement-тесте — это неверно. В коде v31: per_level: 2 × 5 уровней = до 10 вопросов. Исправлено во всех документах.

**Открытых расхождений на 24.06.2026 нет.**

**Выявлено и исправлено в v43 (тестирование):**
- Попап тап-слова в суперквесте не показывал translit/translation для слов с пустыми полями в словаре.
- Суперквест завершался досрочно при обрыве сети на любой сцене кроме первой.
- Медленный старт суперквеста из-за последовательного запроса functional_new и приоритета медленной модели 2.5-flash.

**Выявлено и исправлено в v45 (тестирование):**
- «Слово в контексте» не показывало перевод предложения — пользователь видел только немецкую фразу с пропуском без русского контекста.
- Автоподсказка (скелет слова) показывалась сразу для «трудных» слов, не давая попробовать самому. Заменена на двухуровневую лампочку.
- TTS в «Фразах на каждый день» не работал: конфликт `data-t`/`.tts` vs `data-say`/`replaySay`.
- При релоде страницы или выходе из приложения урок начинался сначала — прогресс внутри урока не сохранялся.

---
